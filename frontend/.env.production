# Backend API origin used by the production build (`npm run build`).
# Must be the exact origin that serves the API and has a valid TLS cert.
# (Your /api/health check succeeded on the www host.)
VITE_API_BASE_URL=https://www.gobus-ai-assistant-backend.goai247.com
